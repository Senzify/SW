# Последние правки: что заменить / убрать / удалить

Ниже короткий чеклист, чтобы обновить макрос до актуальной версии с матрицей свойств (Удалять/Создавать по чекбоксам).

## Полный гайд без сокращений

Если нужен максимально подробный пошаговый процесс именно по созданию новой вкладки, используйте документ:
- `FULL_GUIDE_NEW_TAB_PROPERTIES_MATRIX.md`

---


## 1) Полностью заменить модули

1. Заменить **целиком** модуль `modSettings` содержимым `modSettings.bas`.
2. Заменить **целиком** модуль `modMain` содержимым `modMain.bas`.
3. Заменить **целиком** code-behind формы `frmSettings` содержимым `frmSettings_CODE.txt`.

> Важно: частично не копируйте, чтобы не оставить старые обработчики и поля.

---

## 2) В форме `frmSettings` удалить старые контролы (если остались)

Удалить, если ещё есть:
- `btnDefaults`
- `chkDryRun`
- `lstExceptions`
- `btnAddException`
- `btnRemoveException`
- `btnSortExceptions`

Исключения теперь вводятся только через многострочный `txtExceptionInput`.

---

## 3) В форме `frmSettings` добавить/проверить вкладки MultiPage

Должны быть страницы:
- `pgGeneral`
- `pgRules`
- `pgProcessing`
- `pgPropsMatrix`  ← новая вкладка матрицы свойств

---

## 4) Вкладка `pgPropsMatrix`: добавить контролы для матрицы

### Заголовки
- `lblDelTitle`, `lblCreTitle`
- `lblDelRow_1..3`, `lblCreRow_1..3`
- `lblDelCol_1..11`, `lblCreCol_1..10`

### Чекбоксы "Удалять"
- `chkDel_<class>_<prop>`
- `class`: 1=Деталь, 2=Покупная/исключение, 3=Сборка
- `prop`: 1..11 (11 = "Другие")

Пример полного диапазона:
- `chkDel_1_1 ... chkDel_1_11`
- `chkDel_2_1 ... chkDel_2_11`
- `chkDel_3_1 ... chkDel_3_11`

### Чекбоксы "Создавать"
- `chkCre_<class>_<prop>`
- `class`: 1..3
- `prop`: 1..10 (без "Другие")

Пример полного диапазона:
- `chkCre_1_1 ... chkCre_1_10`
- `chkCre_2_1 ... chkCre_2_10`
- `chkCre_3_1 ... chkCre_3_10`

---

## 5) Проверить имя файла настроек

Файл настроек теперь называется:
- `PropertyN.ini`

Если у вас был старый `Constructor.ini`, можно удалить его вручную или оставить как архив.

---

## 6) Что поменялось в логике

1. В `PropertyN.ini` добавлены секции:
   - `[DeleteMatrix]`
   - `[CreateMatrix]`
2. При включенном `ClearProperties` удаляются только свойства, отмеченные в матрице "Удалять".
3. Создаются только свойства, отмеченные в матрице "Создавать".
4. Пункт "Другие" влияет только на удаление неизвестных/не наших свойств.

---

## 7) Быстрый smoke test

1. Запустите `modMain.main`.
2. На вкладке `Матрица свойств` снимите, например, `chkCre_1_9` (Масса для детали).
3. Нажмите `Применить`, затем `ОК`.
4. Прогоните на детали и проверьте, что `Масса` не создаётся, а остальные выбранные создаются.

